import Link from "next/link";

export default function FeatureCard({title, icon}: {title: string, icon: React.ReactNode}){
    return (
        <Link href="#" className="bg-gray-100 rounded-lg p-5 flex items-center gap-4">
            
        </Link>
    )
}